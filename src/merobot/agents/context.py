# Memory: long term (separate memory not in context), chat memory (conversation history), execution memory (tool calls and results [later: also include the plan and todos and what didn't worked])
# Context: system prompt + conversation history + tool call results (if any)


class AgentContextBuilder:
    def __init__(self):
        pass
